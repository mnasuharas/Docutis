/* Optional educational media registry. No external images are included in Goal 6. */
(function () {
  "use strict";

  window.DOCUTIS_MEDIA = Object.freeze({
    schemaVersion: 1,
    items: Object.freeze([])
  });
}());
